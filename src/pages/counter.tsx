import {Component} from 'react';
import {useSelector} from 'react-redux';

class Counter extends Component {
    render() {
        return (
            <div>
                <button>+</button>
            </div>
        );
    }
}
